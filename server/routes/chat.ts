import { Router } from "express";

const router = Router();

// This file is kept for potential future HTTP endpoints
// All chat functionality is now handled via Socket.IO in socket-chat.ts

export default router;